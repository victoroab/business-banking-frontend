export interface CardPaylod {
  fromAccountNumber: string;
  cardType: string;
  deliveryOption: string;
  pickupBranch: string;
  address: string;
  city: string;
  zipCode: string;
  pin: string;
}
